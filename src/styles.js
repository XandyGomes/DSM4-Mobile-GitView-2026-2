import styled from "styled-components/native";
import { RectButton } from "react-native-gesture-handler";

//estilo da página main
export const Container = styled.View`
    flex: 1;
    padding: 30px
`;

export default Form = styled.View``;

export default Input = styled.TextInput.attrs({})``

export default SubmitButton = styled(RectButton)``

export default List = styled.FlatList.attrs({})``;

export default User
export default Avatar
export default Name
export default Bio
export default ProfileButton
export default ProfileButtonText

